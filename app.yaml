service: default
env: flex
runtime: python

runtime_config:
  python_version: 3

beta_settings:
  cloud_sql_instances: apt-perception-278212:europe-west3:postgres

# 8080 port needs to match the port specified in the app.py
# app:server needs to match the app.py file and the server variable in the app.py
entrypoint: gunicorn -b 0.0.0.0:8080 app:server

env_variables:
  CLOUD_SQL_CONNECTION_NAME:  apt-perception-278212:europe-west3:postgres
  DB_USER: postgres
  DB_PASS: HZoxkCDKTX:N>96QjQ6_
  DB_NAME: postgres
